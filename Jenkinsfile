pipeline {
    agent any
    stages {
        stage("git clone") {
            steps {
                echo "do git clone"
            }
            
        }
        stage("build docker image") {
            steps {
                echo "build image"
            }
        }
        
    }
}